> **PRECONDITION — DO NOT USE BEFORE START FREEZE**
>
> Use only after QUADRA START has a canonical commit/tag, the contract is completed, subscriptions are active, and the user explicitly authorizes BUSINESS.

# Lovable Prompt — QUADRA Studio BUSINESS

## Context

Derive BUSINESS from the canonical QUADRA Studio START codebase.

Record:

```text
START SHA:
START tag:
```

Do not rebuild from zero.

## Objective

Expand the approved identity into a multipage Professional Services site with competencies, sectors, professionals, verified content and a demo-safe qualified request.

## Inherit

Preserve exactly:

- palette;
- fonts;
- scale;
- grid;
- buttons;
- navbar;
- drawer;
- footer;
- image treatment;
- expertise language;
- motion;
- mobile behavior;
- attribution.

## Routes

```text
/
/competenze
/competenze/:slug
/settori
/settori/:slug
/studio
/professionisti
/professionisti/:slug
/insight
/insight/:slug
/richiedi-consulenza
/contatti
/privacy
/cookie
/note-legali
/404
```

Do not create `/casi` or `/area-clienti` unless the approved data explicitly requires them. In the portfolio demo, cases must remain scenario demos and client area must be hidden or demo-only.

## Data

Central typed content for:

- competencies;
- sectors;
- professionals;
- insights;
- navigation;
- disclosures;
- lead adapter;
- SEO.

Use fictional placeholders clearly labeled in code/UI where needed. Do not invent real qualifications.

## Lead flow

Default `demo`.

Steps:

1. profile;
2. area;
3. objective;
4. timing;
5. contact;
6. consent;
7. summary.

Show:

```text
Non inviare documenti, dati sensibili o dettagli riservati.
La richiesta non costituisce accettazione dell'incarico.
```

On submit:

```text
Questa è una demo Tretnix. Nessuna informazione è stata inviata o memorizzata.
```

No network request, storage or upload.

## Professional pages

Use neutral demo profiles only if clearly marked as placeholders; preferably demonstrate layout without invented qualifications. Never invent registers, licenses, years or awards.

## Insight

Demo editorial entries must be labeled as demonstrative and must not contain professional advice.

## Architecture

No backend, auth, database, upload, CRM or portal. No unapproved dependencies.

## SEO

Route-specific metadata, breadcrumbs and safe structured data. No ratings or fake LocalBusiness data.

## Accessibility, responsive, motion and verification

Follow the approved docs and START contract. Run only real checks and report exact evidence.

## Required output

- derivation evidence;
- files;
- inherited patterns;
- new routes/data;
- dependencies;
- checks;
- parity risks;
- manual QA;
- confirmation no backend or sensitive-data flow.
